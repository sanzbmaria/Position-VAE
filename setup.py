# from setuptools import setup, find_packages  

# setup(
#     name='position_vae_project',
#     version='0.0.1',
#     install_requires=[
#         'requests',
#         'importlib-metadata; python_version == "3.10"',
#     ],
# )
# setup(name = 'setup_utils', packages = find_packages())
# setup(name = 'data_utils', packages = find_packages())
